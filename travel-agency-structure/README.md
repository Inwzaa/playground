# ✈️ Travel Agency Landing Page

A semantic HTML practice page demonstrating how to structure a simple business landing page with images and links.

## ⚡ Concepts Practiced
* **Semantic Figures:** Using `<figure>` and `<figcaption>` to attach captions to images meaningfully.
* **External Linking:** Using `target="_blank"` to open links in new tabs safely.
* **List Structure:** Organizing navigation or options using unordered lists (`<ul>`).
* **Alt Text:** Writing descriptive `alt` attributes for accessibility.

## 🛠️ Built With
* HTML5
